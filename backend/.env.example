OPENAI_API_KEY=sk-aqui-tu-clave
LLM_MODEL=gpt-4o-mini
FRONTEND_ORIGIN=http://localhost:5173
MAX_FILE_SIZE_MB=10

# Orquestación / resiliencia (Epic 1)
LLM_MAX_CONCURRENCY=5
RATE_LIMIT=10/minute

# Persistencia de resultados/métricas (Story 1.9)
DATABASE_URL=sqlite:///./hu_analyzer.db

# Panel admin (Epic 3) — generar el hash con:
#   python -c "from app.core.security import hash_password; print(hash_password('TU_PASSWORD'))"
ADMIN_PASSWORD_HASH=
JWT_SECRET=cambia-esto-en-produccion
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
